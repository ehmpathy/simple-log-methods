# roadmap: findsert logstream

executes the blueprint in ordered phases with behavioral verification at each step.

---

## phase 0: dependencies

**deliverables:**
- [ ] install `@aws-sdk/client-cloudwatch-logs` dependency

**acceptance:**
- [ ] `pnpm list @aws-sdk/client-cloudwatch-logs` shows package installed
- [ ] `npm run build` passes

**read before:**
- (none required)

---

## phase 1: domain objects

**deliverables:**
- [ ] create `src/domain.objects/LogOutlet.ts` with `LogOutlet` and `LogEvent` interfaces
- [ ] export `LogOutlet` and `LogEvent` from `src/index.ts`

**acceptance:**
- [ ] `rhx git.repo.test --what types` passes
- [ ] exports are accessible: `import { LogOutlet, LogEvent } from 'simple-log-methods'`

**read before:**
- `3.3.1.blueprint.product.yield.md` — contracts section for `LogOutlet` and `LogEvent` interfaces

---

## phase 2: transformer

**deliverables:**
- [ ] create `src/domain.operations/asLambdaStyleLogGroupName.ts`
- [ ] create `src/domain.operations/asLambdaStyleLogGroupName.test.ts`
- [ ] export `asLambdaStyleLogGroupName` from `src/index.ts`

**acceptance:**
- [ ] `rhx git.repo.test --what unit --scope asLambdaStyleLogGroupName` passes
- [ ] test cases:
  - [ ] returns `/aws/lambda/{service}-{env.access}` format
  - [ ] infers service from package.json when absent
  - [ ] infers env.access from sdk-environment when absent
  - [ ] uses explicit values when provided

**read before:**
- `3.3.1.blueprint.product.yield.md` — contracts section for `asLambdaStyleLogGroupName`
- `3.1.3.research.internal.product.code.prod._.stone` — internal code patterns

---

## phase 3: extend generateLogMethod

**deliverables:**
- [ ] update `src/domain.operations/generateLogMethod.ts` to accept `outlets` and dispatch `LogEvent` to each
- [ ] update `src/domain.operations/generateLogMethod.test.ts` with new tests

**acceptance:**
- [ ] `rhx git.repo.test --what unit --scope generateLogMethod` passes
- [ ] test cases:
  - [ ] extant tests pass (backwards compat)
  - [ ] calls `outlet.send` with `LogEvent`
  - [ ] calls all outlets when multiple provided
  - [ ] warns to console if `outlet.send` throws
  - [ ] does not block on `outlet.send`

**read before:**
- `3.3.1.blueprint.product.yield.md` — contracts section for `generateLogMethod`
- `3.1.3.research.internal.product.code.prod._.stone` — internal code patterns
- `3.1.3.research.internal.product.code.test._.stone` — internal test patterns

---

## phase 4: extend generateLogMethods

**deliverables:**
- [ ] update `src/domain.operations/generateLogMethods.ts` to accept and pass `outlets` to `generateLogMethod`
- [ ] update `src/domain.operations/generateLogMethods.test.ts` with new tests

**acceptance:**
- [ ] `rhx git.repo.test --what unit --scope generateLogMethods` passes
- [ ] test cases:
  - [ ] extant tests pass (backwards compat)
  - [ ] passes outlets to each log method
  - [ ] works without outlets (console only)

**read before:**
- `3.3.1.blueprint.product.yield.md` — contracts section for `generateLogMethods`
- `3.1.3.research.internal.product.code.test._.stone` — internal test patterns

---

## phase 5: cloudwatch outlet

**deliverables:**
- [ ] create `src/domain.operations/genCloudwatchOutlet.ts` with:
  - [ ] `genCloudwatchOutlet` factory
  - [ ] `_findsertLogGroup` internal (catch ResourceAlreadyExistsException)
  - [ ] `_findsertLogStream` internal (catch ResourceAlreadyExistsException)
  - [ ] auto-flush on interval (30s) and process exit hooks
  - [ ] buffer with max size force-flush
- [ ] create `src/domain.operations/genCloudwatchOutlet.integration.test.ts`
- [ ] export `genCloudwatchOutlet` from `src/index.ts`

**acceptance:**
- [ ] `rhx git.repo.test --what integration --scope genCloudwatchOutlet` passes
- [ ] test cases:
  - [ ] findserts log group
  - [ ] findserts log stream
  - [ ] sends log event to cloudwatch
  - [ ] auto-flushes on interval
  - [ ] batches events (max 1MB)
  - [ ] throws if AWS credentials absent
  - [ ] skips log group creation if `skipLogGroupCreation: true`
  - [ ] snapshots LogEvent structure
  - [ ] snapshots error when AWS_REGION absent

**read before:**
- `3.3.1.blueprint.product.yield.md` — contracts section for `genCloudwatchOutlet`
- `3.1.1.research.external.product.flagged._.stone` — CloudWatch limits and cost
- `3.1.3.research.internal.product.code.test._.stone` — internal test patterns

---

## phase 6: acceptance test

**deliverables:**
- [ ] create `src/domain.operations/genCloudwatchOutlet.journey.acceptance.test.ts`

**acceptance:**
- [ ] `rhx git.repo.test --what acceptance --scope genCloudwatchOutlet` passes
- [ ] test cases (full journey):
  - [ ] `[case1]` cloudwatch outlet journey
    - [ ] `[t0]` outlet created → log group and stream findserted
    - [ ] `[t1]` log messages → buffered
    - [ ] `[t2]` auto-flush fires → logs in cloudwatch
    - [ ] `[t3]` log more → new messages buffered
    - [ ] `[t4]` exit hook fires → rest flushed
    - [ ] `[t5]` verify → all logs present in order (snapshot)

**read before:**
- `3.3.1.blueprint.product.yield.md` — test tree section
- `2.1.criteria.blackbox.stone` — blackbox acceptance criteria

---

## phase 7: verification

**deliverables:**
- [ ] all tests pass
- [ ] types pass
- [ ] lint passes
- [ ] build passes

**acceptance:**
- [ ] `rhx git.repo.test --what types` passes
- [ ] `rhx git.repo.test --what lint` passes
- [ ] `rhx git.repo.test --what unit` passes
- [ ] `rhx git.repo.test --what integration` passes
- [ ] `rhx git.repo.test --what acceptance` passes
- [ ] `npm run build` passes

**read before:**
- (none required)

---

## dependency order

```
phase 0 (deps)
    │
    ▼
phase 1 (domain objects)
    │
    ▼
phase 2 (transformer)
    │
    ├───────────────────┐
    ▼                   ▼
phase 3              phase 5
(generateLogMethod)  (genCloudwatchOutlet)
    │                   │
    ▼                   │
phase 4                 │
(generateLogMethods)    │
    │                   │
    └───────────────────┘
            │
            ▼
      phase 6 (acceptance)
            │
            ▼
      phase 7 (verification)
```

phases 3-4 and phase 5 can run in parallel after phase 2 completes.
