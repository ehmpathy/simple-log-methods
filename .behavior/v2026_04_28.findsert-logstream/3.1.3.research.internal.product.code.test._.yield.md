# internal research: test code patterns

research of extant test patterns relevant to the findsert logstream feature.

---

## pattern.1: jest unit test structure

**location:** src/domain.operations/generateLogMethods.test.ts:1-12 [1]

**what it does:**
- uses standard jest `describe` blocks
- uses `it` for test cases
- simple property assertions with `toHaveProperty`

**relevance:** [REUSE] — same pattern for test of generateLogMethods with outlets

**citation [1]:**
```typescript
import { generateLogMethods } from './generateLogMethods';

describe('generateLogMethods', () => {
  it('should create the log methods', () => {
    const log = generateLogMethods();
    expect(log).toHaveProperty('error');
    expect(log).toHaveProperty('warn');
    expect(log).toHaveProperty('info');
    expect(log).toHaveProperty('debug');
  });
});
```

---

## pattern.2: jest.spyOn for console methods

**location:** src/domain.operations/generateLogMethod.test.ts:7-14 [2]

**what it does:**
- uses `jest.spyOn` to observe console.log and console.warn calls
- uses `beforeEach` to clear mocks between tests
- asserts call count and arguments

**relevance:** [REUSE] — same pattern for verification of console output when outlets are set

**citation [2]:**
```typescript
describe('generateLogMethod', () => {
  beforeEach(() => jest.clearAllMocks());
  it('should generate a method that outputs to console.log if below warn level', () => {
    const spy = jest.spyOn(console, 'log');
    const logError = generateLogMethod({
      level: LogLevel.INFO,
      minimalLogLevel: LogLevel.DEBUG,
    });
    logError('testMessage');
    expect(spy).toHaveBeenCalledTimes(1);
  });
```

---

## pattern.3: jest.mock for dependencies

**location:** src/domain.operations/formatLogContentsForEnvironment.test.ts:6-8 [3]

**what it does:**
- uses `jest.mock` to mock internal dependencies
- casts mock to `jest.Mock` for type-safe mock control
- uses `mockReturnValue` to control return values per test

**relevance:** [AVOID] — for outlet tests, prefer dependency injection over mocks

**citation [3]:**
```typescript
jest.mock('./identifyEnvironment');
const identifyEnvironmentMock = identifyEnvironment as jest.Mock;

describe('formatMetadataForEnvironment', () => {
  it('should not stringify the contents - but should stringify the metadata - if in the local environment', () => {
    identifyEnvironmentMock.mockReturnValue(SupportedEnvironment.LOCAL);
```

---

## pattern.4: mock LogMethods helper

**location:** src/domain.operations/withLogTrail.test.ts:6-16 [4]

**what it does:**
- creates mock LogMethods object with jest.fn() for each level
- enables verification of which log level was called
- returns typed object with mock methods

**relevance:** [REUSE] — same pattern to test outlet dispatch behavior

**citation [4]:**
```typescript
const createMockLogMethods = (): LogMethods & {
  debug: jest.Mock;
  info: jest.Mock;
  warn: jest.Mock;
  error: jest.Mock;
} => ({
  debug: jest.fn(),
  info: jest.fn(),
  warn: jest.fn(),
  error: jest.fn(),
});
```

---

## pattern.5: expect.objectContaining for partial matches

**location:** src/domain.operations/generateLogMethod.test.ts:33-39 [5]

**what it does:**
- uses `expect.objectContaining` for partial object assertions
- verifies specific properties without match of entire object
- uses `expect.any(Object)` for dynamic values

**relevance:** [REUSE] — same pattern to verify LogEvent structure to outlets

**citation [5]:**
```typescript
expect(spy).toHaveBeenCalledWith(
  expect.objectContaining({
    level: LogLevel.ERROR,
    message: 'testMessage',
    metadata: JSON.stringify({ nested: { object: true } }),
  }),
);
expect(spy.mock.calls[0]![0]).toHaveProperty('timestamp');
```

---

## pattern.6: jest configuration structure

**location:** jest.unit.config.ts:13-42 [6]

**what it does:**
- separates unit, integration, and acceptance tests via file patterns
- uses `@src` path alias via moduleNameMapper
- uses swc for fast transpilation
- sets UTC timezone for consistent snapshots

**relevance:** [REUSE] — outlet integration tests will use jest.integration.config.ts

**citation [6]:**
```typescript
const config: Config = {
  verbose: true,
  testEnvironment: 'node',
  moduleNameMapper: {
    '^@src/(.*)$': '<rootDir>/src/$1',
  },
  transform: {
    '^.+\\.(t|j)sx?$': '@swc/jest',
  },
  testMatch: [
    '**/*.test.ts',
    '!**/*.acceptance.test.ts',
    '!**/*.integration.test.ts',
  ],
  setupFilesAfterEnv: ['./jest.unit.env.ts'],
};
```

---

## pattern.7: integration test timeout

**location:** jest.integration.env.ts:7 [7]

**what it does:**
- sets 90 second timeout for integration tests
- accounts for downstream API calls

**relevance:** [REUSE] — CloudWatch outlet integration tests need timeout for API calls

**citation [7]:**
```typescript
jest.setTimeout(90000); // since we're calling downstream apis
```

---

## pattern.8: integration test environment validation

**location:** jest.integration.env.ts:26-51 [8]

**what it does:**
- validates test environment is 'test' stage
- validates AWS credentials if required by declapract.use.yml
- fails fast with clear error messages

**relevance:** [EXTEND] — CloudWatch outlet tests need AWS credentials validation

**citation [8]:**
```typescript
if (
  (process.env.NODE_ENV !== 'test' ||
    (process.env.STAGE && process.env.STAGE !== 'test')) &&
  process.env.I_KNOW_WHAT_IM_DOING !== 'true'
)
  throw new Error(`integration.test is not targeting stage 'test'`);

const requiresAwsAuth = declapractUseContent.includes('awsAccountId');
if (
  requiresAwsAuth &&
  !(process.env.AWS_PROFILE || process.env.AWS_ACCESS_KEY_ID)
)
  throw new Error(
    'no aws credentials present. please authenticate with aws to run integration tests',
  );
```

---

## pattern.9: async test patterns

**location:** src/domain.operations/withLogTrail.test.ts:270-324 [9]

**what it does:**
- uses async/await in test functions
- uses `expect().rejects.toThrow()` for async errors
- verifies behavior that matches sync counterparts

**relevance:** [REUSE] — outlet flush operations are async

**citation [9]:**
```typescript
describe('async functions', () => {
  it('should use specified levels for async function input', async () => {
    const mockLog = createMockLogMethods();
    const testFn = async function testAsyncFunction(input: string) {
      return input.toUpperCase();
    };
    const wrapped = withLogTrail(testFn, {
      log: { level: { input: LogLevel.INFO } },
    });

    await wrapped('hello', { log: mockLog });

    expect(mockLog.info).toHaveBeenCalledWith(
      'testAsyncFunction.input',
      expect.any(Object),
    );
  });
```

---

## summary

| pattern | action | rationale |
|---------|--------|-----------|
| jest unit test structure | [REUSE] | standard pattern |
| jest.spyOn for console | [REUSE] | verify console output |
| jest.mock for deps | [AVOID] | prefer DI for outlets |
| mock LogMethods helper | [REUSE] | verify outlet dispatch |
| expect.objectContaining | [REUSE] | partial LogEvent match |
| jest config structure | [REUSE] | use integration config |
| integration timeout | [REUSE] | cloudwatch API calls |
| environment validation | [EXTEND] | AWS credential check |
| async test patterns | [REUSE] | outlet flush is async |

---

## new test patterns needed

for the outlet feature, we will need:

1. **unit tests** for:
   - `generateLogMethods` with outlets param
   - `generateLogMethod` with outlet dispatch
   - `LogOutlet` interface compliance
   - `asLambdaStyleLogGroupName` transformer

2. **integration tests** for:
   - `genCloudwatchOutlet` with real AWS CloudWatch
   - log group findsert behavior
   - log stream creation
   - auto-flush on process exit
   - credential validation

3. **test infrastructure**:
   - mock outlet factory for unit tests
   - AWS credential validation in jest.integration.env.ts
