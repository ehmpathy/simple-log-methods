wish =

we want to ensure that if we're not in an environment which automatically collects logs (e.g., aws lambda)

then this library will enable us to findsert a logstream and force collect logs into aws cloudwatch anyway

e.g., findsert logstream and etc

---

an important consideratino is tht we want to ensure to reuse extant patterns for loggroup creation

i.e., maybe we just assume the lambda template and infer the log group pattern based on standard pattern plus package name

we can also import environment requests if needed too

---

                       
❯ couple main questions                                                                                                      
                                                                                                                             
1. how will we ensure that hte logstream names match the default for aws lambda?                                             
2. how will we make it clear that we only support aws lambda? (and extend future support for other endpoints)                
3. how will we only do this if there is not already a forwarder installed on the machine?                                    
                                                                                                                             
                                                                                                                             
also, could we consider this as a supplier that we allow folks to optionally pass into genContextLogTrail                    
                                                                                                                             
so that if they pass in this transport supplier, we will also route logs into that transport?                                

