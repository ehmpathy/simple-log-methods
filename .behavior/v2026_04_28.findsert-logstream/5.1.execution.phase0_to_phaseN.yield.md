# execution: phase0 to phaseN

tracks progress through the roadmap phases.

---

## phase 0: dependencies

- [x] install `@aws-sdk/client-cloudwatch-logs` dependency
- [x] verify `npm run build` passes

---

## phase 1: domain objects

- [x] create `src/domain.objects/LogOutlet.ts`
- [x] export from `src/index.ts`
- [x] verify types pass

---

## phase 2: transformer

- [x] create `src/domain.operations/asLambdaStyleLogGroupName.ts`
- [x] create `src/domain.operations/asLambdaStyleLogGroupName.test.ts`
- [x] export from `src/index.ts`
- [x] verify unit tests pass

---

## phase 3: extend generateLogMethod

- [x] update `src/domain.operations/generateLogMethod.ts`
- [x] update `src/domain.operations/generateLogMethod.test.ts`
- [x] verify unit tests pass

---

## phase 4: extend generateLogMethods

- [x] update `src/domain.operations/generateLogMethods.ts`
- [x] update `src/domain.operations/generateLogMethods.test.ts`
- [x] verify unit tests pass

---

## phase 5: cloudwatch outlet

- [x] create `src/domain.operations/genCloudwatchOutlet.ts`
- [x] create `src/domain.operations/genCloudwatchOutlet.integration.test.ts`
- [x] export from `src/index.ts`
- [x] verify integration tests pass

---

## phase 6: acceptance test

- [x] create `src/domain.operations/genCloudwatchOutlet.journey.acceptance.test.ts`
- [x] verify acceptance tests pass

---

## phase 7: verification

- [x] `rhx git.repo.test --what types` passes
- [x] `rhx git.repo.test --what lint` passes
- [x] `rhx git.repo.test --what unit` passes (45 tests)
- [ ] `rhx git.repo.test --what integration` — **blocked: AWS credentials expired**
- [ ] `rhx git.repo.test --what acceptance` — **blocked: AWS credentials expired**
- [x] `npm run build` passes

### integration/acceptance blocker

tests require valid AWS credentials to create CloudWatch log groups/streams.

**error**: `CredentialsProviderError: Your session has expired. Please reauthenticate.`

**fix**: human needs to refresh AWS SSO session:
```bash
aws sso login --profile <your-profile>
```

or add AWS credentials to keyrack:
```bash
rhx keyrack fill --owner ehmpath
```

**note**: keyrack currently has no AWS credentials for `ehmpathy.test`

---

## phase 8: review blocker fixes

### blockers addressed

- [x] **events lost on flush failure (r3)**: buffer now restored on API failure via `buffer.unshift(...events)`
- [x] **no cleanup method (r3)**: added `close()` method to LogOutlet interface and genCloudwatchOutlet
- [x] **no test for deprecation notice (r7)**: added test case for `minimalLogLevel` deprecation console.warn
- [x] **decode-friction in orchestrator (r6/r7)**: extracted `drainBuffer.ts` transformer from inline `buffer.splice` in genCloudwatchOutlet
- [x] **no fail-fast credential validation (r7)**: added `assertAwsCredentialsPresent.ts` — validates AWS credentials at outlet creation, not first flush
- [x] **usecase.5 conflict resolved**: updated criteria to match user's explicit fail-fast requirement — outlet errors propagate and crash process
- [x] **async signal handler race (r3)**: rewrote signal handlers to use sync wrapper with promise chain, explicit `process.exit()` after flush
- [x] **void flush() implicit crash (r3)**: replaced with explicit `.catch(() => process.exit(1))`
- [x] **inline decode-friction (r3)**: extracted `asCurrentIsoTimestamp.ts` from `new Date().toISOString()`
- [x] **missing transformer tests (r3)**: added tests for `asCloudWatchLogEvents`, `computeLogEventSize`, `drainBuffer`, `asCurrentIsoTimestamp`
- [x] **misleading O(1) docstring (r3)**: clarified that function is O(n), benefit is amortized via incremental sum

### deferred or N/A

- **flush interval mismatch (r7)**: vision says 30s, implementation uses 5s
  - **resolution**: intentional per user request — user explicitly asked for smaller buffer limits with faster emission (5s interval, 256KB buffer)
  - **note**: route is sealed, cannot update vision stone; implementation reflects actual user intent

- **sdk-environment not used (r7)**: env.access inferred via raw env vars instead of sdk-environment package
  - **resolution**: deferred — current inline env var checks are functionally equivalent; sdk-environment integration would add dependency for minimal benefit in this context

- **genContextLogTrail integration (r7 nitpick)**: wish suggested outlets as supplier to genContextLogTrail
  - **resolution**: deferred for future iteration — current design passes outlets to generateLogMethods which achieves the goal
